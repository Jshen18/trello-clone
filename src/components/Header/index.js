import React from 'react';
import { StyledNav, StyledTitle } from './styles';

const Header = () => (
  <StyledNav>
    <StyledTitle>TrellerBoardz</StyledTitle>
  </StyledNav>
);


export default Header;